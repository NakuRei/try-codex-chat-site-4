# 検証記録

## 確認環境と制約

作成時のローカルChromiumをPlaywrightで操作し、同一ソースのHTML/CSS/JavaScriptをブラウザーのDOMに読み込んで検証しました。作業環境ではHTTP/file URLへのブラウザー遷移が制限されているため、公開URL経由の配信確認とは区別しています。

保存データの復元テストはStorage APIのテストダブルを使い、保存したJSONを新しいドキュメントに渡して確認しました。実環境のlocalStorageを使ったリロード・ブラウザー再起動後の保持、および公開後のHTTP配信は、別途確認が必要です。テストダブルはサイト本体には含みません。

## 実施して通過したチェック

- `node --check app.js` によるJavaScriptの構文チェック。
- トップページの描画、初期状態で音がOFFであること。
- 5種類の謎をUIから解き、かけらを5個集めてエンディングに到達する通し操作。
- 誤答時にかけらを取得できないこと、再挑戦できること。
- かけら不足で秘密のページを開けないこと、合言葉の誤答判定、全角・カタカナ・空白の正規化。
- BBSの全角キリ番受付、HTML/スクリプトに見える入力を文字列として表示すること。
- 工事中パズルの配置リセット。
- 星の合図を表示中に別ページへ移動した場合のタイマー停止。
- 保存データを新しいドキュメントに渡したときの、かけらとクリア状態の復元。
- 記録削除の確認・キャンセル・実行、BBSのローカル投稿の削除。
- 3段階のヒント、最後の段階での追加ボタン無効化、Escapeによるダイアログ終了。
- ユーザー操作によるWeb AudioのON/OFF。
- 10画面を横幅320 / 375 / 390 / 620 / 768 / 1024 / 1440pxで表示し、ページ全体に横方向のはみ出しがないこと。
- タッチ操作および動きを減らす設定での星パズル。
- 保存APIが例外を投げる環境でのメモリー内プレイ継続。
- 壊れた保存JSONからの初期状態への復帰。
- 上記操作中のブラウザーJavaScript例外が0件であること。

デスクトップ・スマートフォン幅のスクリーンショットを取得し、トップページ、日記、工事中画面などの表示も確認しました。実機の全ブラウザーを網羅した保証ではありません。

## 公開後の確認手順

1. Pagesの公開URLを開き、HTML・CSS・JavaScript・faviconが正常に配信されることを確認する。
2. 1つかけらを取得し、ページ再読み込みで残ることを確認する。
3. BBSに足跡を残し、再読み込みで残ることを確認する。別ブラウザーには共有されないことも確認する。
4. 音をON/OFFし、タブを切り替えると音が停止することを確認する。
5. スマートフォンのタッチ操作で各ページと入力フォームを確認する。

謎ときの解法は、ゲーム内の3段階目のヒントで確認できます。
